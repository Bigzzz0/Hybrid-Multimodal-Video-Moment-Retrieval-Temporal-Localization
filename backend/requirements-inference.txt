# Install this file in the dedicated Python 3.12 worker environment.
# The main API environment remains compatible with the existing project.
torch==2.10.0
torchvision
transformers>=4.57.0
accelerate>=0.27.0
bitsandbytes>=0.43.0
qwen-vl-utils>=0.0.8
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
httpx>=0.27.0
numpy>=1.26,<2
pillow>=10.2.0
timm>=1.0.17
tqdm
ftfy==6.1.1
regex
iopath>=0.1.10
huggingface_hub
einops>=0.8.0
pycocotools>=2.0.7
# SAM 3.1 imports Triton at module load; use the Windows-compatible wheel.
triton-windows>=3.8.0; platform_system == "Windows"

# Install the latest SAM3 source separately so the SAM3.1 checkpoint and code
# stay in lockstep:
# pip install -e <path-to-sam3>
